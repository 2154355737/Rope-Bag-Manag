# 生产环境配置
# 文件名: .env.production
# 此文件在构建生产版本时自动加载

# API基础URL - 生产环境使用相对路径，通过nginx代理
# 使用代码中的默认值 /api，不在环境变量中重复设置
# VITE_API_BASE_URL=/api

# 可选：直接指向后端服务器（适用于无nginx代理的场景）
# VITE_API_BASE_URL=http://39.105.113.219:15201/api
# VITE_API_BASE_URL=http://your-domain.com:15201/api

# 应用配置
VITE_APP_TITLE=绳包管理器
VITE_APP_VERSION=1.0.0
VITE_ENV=production

# 临时启用生产环境调试（用于问题排查）
VITE_DEBUG_API=true 