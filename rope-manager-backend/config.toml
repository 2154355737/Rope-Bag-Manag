# 绳包管理器后端配置文件
# 服务器配置
[server]
host = "127.0.0.1"
port = 15201
workers = 4
timeout = 30

# 数据库配置
[database]
url = "data.db"
max_connections = 10
timeout = 30

# 认证配置
[auth]
jwt_secret = "your-secret-key-change-in-production"
jwt_expiration = 86400  # 24小时
bcrypt_cost = 12

# 文件配置
[file]
upload_path = "uploads"
max_file_size = 10485760  # 10MB
allowed_extensions = ["zip", "rar", "7z", "tar", "gz"]
temp_path = "temp"

# 日志配置
[logging]
level = "info"
file_path = "logs/app.log"
max_files = 5
max_size = 10485760  # 10MB

# CORS配置
[cors]
allowed_origins = ["*"]
allowed_methods = ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
allowed_headers = ["Content-Type", "Authorization", "X-Requested-With"]
max_age = 3600

# 邮件配置 (需要配置真实的SMTP信息才能发送邮件)
[mail]
smtp_server = "smtp.163.com"        # QQ邮箱SMTP服务器
smtp_port = 25                    # QQ邮箱SSL端口
username = "siguail196@163.com"     # 发送方邮箱地址
password = "WLVu8KRNc6KE62Ls"       # SMTP授权码(不是登录密码)
from_name = "绳包管理器"            # 发送方显示名称 