# 环境变量配置示例
# 请根据实际部署环境创建对应的 .env.local 文件

# =============================================================================
# API服务器配置
# =============================================================================

# 开发环境 - 使用Vite代理，API请求会被代理到后端服务器
# VITE_API_BASE_URL=/api

# 生产环境/Tauri构建 - 需要完整的服务器地址
# VITE_API_BASE_URL=http://39.105.113.219:15201/api/v1

# 本地开发环境（如果后端也在本地运行）
# VITE_API_BASE_URL=http://localhost:15201/api/v1

# =============================================================================
# 应用配置
# =============================================================================
VITE_APP_TITLE=资源社区APP
VITE_APP_VERSION=1.1.0

# =============================================================================
# 网络配置
# =============================================================================
# 请求超时时间（毫秒）
VITE_REQUEST_TIMEOUT=15000

# =============================================================================
# 开发配置
# =============================================================================
# 调试模式（仅在开发环境使用）
VITE_DEBUG=true

# =============================================================================
# 使用说明
# =============================================================================
# 1. 复制此文件为 .env.local
# 2. 根据你的部署环境修改 VITE_API_BASE_URL
# 3. 对于Tauri Android构建，必须使用完整的服务器地址（包含协议、IP/域名、端口）
# 4. 开发环境可以使用相对路径 /api，会通过Vite代理转发 